# ansible

* automate golang setup
* add alacritty role into playbook once it works
* bat config symlink with stow doesnt yet work
* bat theme using anisble

# tmux

* experiment with storing/reattaching to the last session or multiple ones I
frequently work with
