# ansible

* automate golang setup
* automate / setup lua-lang with LSP
* JAVA_HOME?
* using command is preferred over shell unless shell expansion is needed

# tmux

* experiment with storing/reattaching to the last session or multiple ones I
frequently work with
