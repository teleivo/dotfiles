---
# Google Chrome manages its own APT repository and signing key via its
# post-install hooks and /etc/cron.daily/google-chrome.
- name: Install Google Chrome
  become: true
  apt:
    pkg:
      - google-chrome-stable
    state: "{{ apt_package_state }}"
