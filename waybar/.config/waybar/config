{
    "layer": "top",
    "position": "top",

    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["clock"],
    "modules-right": ["custom/dictation", "network", "bluetooth", "pulseaudio", "backlight", "battery", "custom/logout"],

    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{index}"
    },

    "sway/mode": {
        "format": "<span style=\"italic\">{}</span>"
    },

    "clock": {
        "format": "{:%H:%M}",
        "format-alt": "{:%Y-%m-%d (Week %V)}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}% 󱊢",
        "format-charging": "{capacity}% 󰂄",
        "format-plugged": "{capacity}% 󰂄",
        "format-full": "{capacity}% 󱊣",
        "on-click": "gnome-control-center power"
    },

    "network": {
        "format-wifi": " ",
        "format-ethernet": "",
        "format-disconnected": "󰖪",
        "on-click": "gnome-control-center wifi"
    },

    "pulseaudio": {
        "format": "{format_source} {volume}% {icon}",
        "format-bluetooth": "{format_source} {volume}% 󰋋",
        "format-bluetooth-muted": "{format_source} 󰝟",
        "format-muted": "{format_source} 󰝟",
        "format-source": "󰍬 {volume}%",
        "format-source-muted": "󰍭",
        "format-icons": {
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol",
        "on-click-right": "pactl set-sink-mute @DEFAULT_SINK@ toggle"
    },

    "backlight": {
        "device": "intel_backlight",
        "format": "{percent}% ◑",
        "on-scroll-up": "brightnessctl set +5%",
        "on-scroll-down": "brightnessctl set 5%-",
        "on-click": "gnome-control-center display"
    },

    "bluetooth": {
        "format": "󰂯",
        "format-connected": "󰥰",
        "format-disabled": "󰂲",
        "interval": 5,
        "tooltip-format": "{controller_alias}\t{controller_address}",
        "tooltip-format-connected": "{controller_alias}\t{controller_address}\n\n{device_enumerate}",
        "tooltip-format-enumerate-connected": "{device_alias}\t{device_address}",
        "on-click": "blueman-manager",
        "on-click-right": "rfkill toggle bluetooth"
    },

    "custom/dictation": {
        "exec": "if [ -f /tmp/dictation-status ] && [ \"$(cat /tmp/dictation-status)\" = \"active\" ]; then echo '●'; else echo ''; fi",
        "interval": 1,
        "tooltip": "Dictation Status - Click to toggle",
        "on-click": "~/code/dotfiles/bin/dictation-toggle.sh"
    },

    "custom/logout": {
        "format": "⎋",
        "tooltip": "Logout",
        "on-click": "wlogout"
    }
}
