---
- name: Install waybar
  become: true
  apt:
    pkg:
      - waybar
      - stow
    state: present

- name: Ensure config folder exists
  file:
    path: ~/.config/waybar
    state: directory

- name: Create symlinks into dotfiles
  command: stow --target ~/ waybar
  args:
    chdir: ~/code/dotfiles
