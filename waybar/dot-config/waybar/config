{
    "layer": "top",
    "position": "top",

    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["clock"],
    "modules-right": ["custom/dictation", "custom/dnd", "network", "pulseaudio", "backlight", "battery", "custom/logout"],

    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{index}"
    },

    "sway/mode": {
        "format": "<span style=\"italic\">{}</span>"
    },

    "clock": {
        "format": "{:%H:%M}",
        "format-alt": "{:%Y-%m-%d (Week %V)}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}% 󱊢",
        "format-charging": "{capacity}% 󰂄",
        "format-plugged": "{capacity}% 󰂄",
        "format-full": "{capacity}% 󱊣",
        "on-click": "gnome-control-center power"
    },

    "network": {
        "format-wifi": " ",
        "format-ethernet": "",
        "format-disconnected": "󰖪",
        "on-click": "gnome-control-center wifi"
    },

    "pulseaudio": {
        "format": "{format_source} {volume}% {icon}",
        "format-bluetooth": "{format_source} {volume}% 󰋋",
        "format-bluetooth-muted": "{format_source} 󰝟",
        "format-muted": "{format_source} 󰝟",
        "format-source": "󰍬 {volume}%",
        "format-source-muted": "󰍭",
        "format-icons": {
            "default": ["", "", ""]
        },
        "scroll-step": 1,
        "max-volume": 80,
        "server": "unix:/run/user/1000/pulse/native",
        "on-click": "pavucontrol",
        "on-click-right": "pactl set-sink-mute @DEFAULT_SINK@ toggle"
    },

    "backlight": {
        "device": "intel_backlight",
        "format": "{percent}% ◑",
        "on-scroll-up": "brightnessctl set +5%",
        "on-scroll-down": "brightnessctl set 5%-",
        "on-click": "gnome-control-center display"
    },


    "custom/dnd": {
        "interval": "once",
        "return-type": "json",
        "format": "{icon}",
        "format-icons": {
            "default": "󰂜",
            "dnd": "󰂛"
        },
        "exec": "~/.local/bin/notify-toggle status",
        "on-click": "~/.local/bin/notify-toggle toggle",
        "signal": 11
    },

    "custom/dictation": {
        "format": "{}",
        "exec": "STATUS_FILE=\"${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/dictation/status\"; if [ -f \"$STATUS_FILE\" ] && [ \"$(cat \"$STATUS_FILE\")\" = \"active\" ]; then echo '{\"text\": \"●\", \"class\": \"active\"}'; else echo '{\"text\": \"\", \"class\": \"inactive\"}'; fi",
        "return-type": "json",
        "interval": 1,
        "tooltip": "Dictation Status - Click to toggle",
        "on-click": "~/code/dotfiles/bin/bin/dictation-toggle.sh"
    },

    "custom/logout": {
        "format": "⎋",
        "tooltip": "Logout",
        "on-click": "wlogout"
    }
}
