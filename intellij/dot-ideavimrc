set number
set relativenumber
set highlightedyank
set textwidth = 100
set nowrap

" TODO does not work
" https://youtrack.jetbrains.com/issue/VIM-1337/Incorrect-leader-key-handling-in-ideavim.
" nnoremap <SPACE> <Nop>
" let mapleader=" "

" sync clipboard between OS and IdeaVim
" uses CLIPBOARD (Ctrl+V) for yanks/deletes
" IdeaVim limitation: can't auto-copy visual selection to PRIMARY (middle-click)
" like terminal Vim does - it's all or nothing with the clipboard setting
set clipboard+=unnamedplus
set clipboard-=autoselect

" search options
" stop highlighting when I am done searching
set nohlsearch
" highlight search results while typing
set incsearch
" case-insensitive searching UNLESS \C or capital in search
set ignorecase
set smartcase

" Plugins
set surround

" TODO fix once the leader mapping works
" Refactoring
" map <leader>rn <Action>(RenameElement)
