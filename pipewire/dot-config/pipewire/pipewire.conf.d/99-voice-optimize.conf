context.properties = {
    # Optimize for voice processing
    default.clock.rate          = 48000
    default.clock.quantum       = 256       # Lower latency
    default.clock.min-quantum   = 32        # Minimum buffer
    default.clock.max-quantum   = 1024      # Maximum buffer
}